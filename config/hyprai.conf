# ══════════════════════════════════════════════════════════════════════
# Hypr.AI · Preferências
# Formato: chave = valor   (# comenta; linha vazia ou valor vazio = padrão)
#
# O install.sh nunca sobrescreve este arquivo — como o tools.conf e o
# sites.conf, a tua versão sobrevive às atualizações.
# ══════════════════════════════════════════════════════════════════════

# Terminal onde os agentes CLI (e editores de terminal) abrem.
# Vazio = o primeiro que existir, nesta ordem: kitty, ghostty, foot,
# alacritty, wezterm. Um terminal fora da lista recebe "-e <comando>".
# A variável de ambiente HYPRAI_TERMINAL, se definida, ganha a isto.
terminal =
